# 変更履歴 (Changelog)

Yui Agent の主な変更点を記録します。

---

## 0.2.0 (2026-06-13)

### ✨ 新機能

#### ゆい MCP サーバ
Claude Code / Codex 等の MCP クライアントから、ゆいのノート・TODO・リマインダーを直接操作し、作業の進捗連絡をゆいの声で受け取れるようになりました。

- リモート **Streamable HTTP** の MCP サーバを `/api/mcp` に公開
- 公開ツール: **ノート / TODO / リマインダーの作成・読取・検索・更新 + ソフト削除** (archive / complete / disable。物理削除は MCP からは不可)
- **`notify_master`**: MCP クライアント → ゆいが**発話 + 通知トースト**でご主人様に報告。文面整形はローカル LLM 優先 + サブモデル fallback。挙動は設定 → 通知タブ「作業連絡 (MCP)」で調整可
- **認証**: MCP 専用トークンを自動生成し、設定 → 連携タブに `claude mcp add` スニペットを表示・ローテート。トークンは AES-256-GCM で at-rest 暗号化、Bearer + Origin/Host 検証で保護

#### ノート機能 (Notes)
markdown メモを保存・意味検索できる「ノート空間」を追加。

- `notes` / `note_chunks` テーブル + 本文の chunk 分割埋め込み (pgvector)
- **ハイブリッド検索** (意味 + 全文) + Notes Modal (検索 / 無限スクロール / インライン編集 / アーカイブ / プロジェクト紐付け)
- `save_note` / `search_notes` ツール。チャットで「メモして」と言うと保存され、**ReportPanel に live 表示 + クリックで該当ノートをモーダルで開ける**

#### TTS 辞書 v2
- **Aho-Corasick** による内部辞書置換の高速化
- **英語→カタカナ一括辞書** (e2k、124,922 件) + 13 万件規模に対応したサーバ検索 + 無限スクロール UI

### 🐛 バグ修正

- **サーバ注入の内部ディレクティブ統一**: ゆいが正規の作業連絡 (ツール呼び忘れ時の自己点検メモ等) を「外部からの誘導」と誤判定して拒否する不具合を修正。サーバが会話に注入する内部制御メッセージを `<yui_directive>` タグ + 専用 system guard に一本化し、leak 可能な自己言及テキストを会話本文から排除。specialist runner も同方式に統一。
- **VRM アップロード**: 10MB を超える VRM のアップロードが 500 になる問題を修正 (上限 70MB + サイズ超過時の明示エラー)。
- **TTS 辞書**: ASCII 単語境界マッチで cmudict が暴発する問題を修正。

### 🔧 その他

- 追加プロンプトのプリセット例 (寄り添い / スパルタ) を同梱。

---

## 0.1.0

初回公開リリース。
